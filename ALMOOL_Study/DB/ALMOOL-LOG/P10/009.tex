\begin{GPTQ}
    이 문제는 뭘 구하는거야   
\end{GPTQ}

\begin{GPTA}
주어진 문제는 정적분 문제입니다. 이것은 주어진 함수 $4 x^3-x$ 에 대해 구간 $[1,2]$ 에서의 면적을 구하는 것입니다.

    정적분은 주어진 구간 내에서 함수의 그래프 아래에 있는 전체 면적을 계산하는 방법입니다. 문제의 실질적인 목표는 이 함수가 주어진 구간 사이에서 어떻게 변하는지를 파악하고, 그에 따라 두 점 사이의 함수 값의 차이를 계산하는 것입니다.

    정리하자면, $\int_1^2\left(4 x^3-x\right), d x$ 는 다음을 의미 합니다:

    - $F(x)$ 는 $4 x^3-x$ 의 원시 함수 또는 부정적분입니다.

    - 구간 $[1,2]$ 에서의 $F(x)$ 값의 차이를 계산하여, 주어진 함수 그래프 아래의 전체 면적을 구하는 것이 목표입니다.
    
    결과적으로, 이 문제는 합수 그래프와 $x$-축 사이의 구간 $[1,2]$ 의 전쳉 면적을 구하는 것입니다.

    결과는 $\frac{27}{2}$ 입니다.
   
\end{GPTA}

