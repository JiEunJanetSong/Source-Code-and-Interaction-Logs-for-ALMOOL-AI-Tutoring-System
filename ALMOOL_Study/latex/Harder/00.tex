```source

```

```template
opening paragrpah\begin{alignat*}{2}%display eq 1
eq
&= eq  \qquad && \{ reason \} \\
&= eq   && \{ reason \} \\
..
\end{alignat*}
수식과 수식 사이에 들어갈 연결 서술 문단 (뒤에 나올 수식을 쓰는 이유를 간략히 설명함)\begin{alignat*}{2}%display eq 2
eq
&= eq  \qquad && \{ reason \} \\
&= eq  && \{ reason \} \\
..
\end{alignat*}
concluding paragraph
```

 
Source의 solution 부분을 template에 맞추어 latex 문법에 맞게 고치되, 오프닝, 연결, 결론 문장은 한 두 문장 정도로만 간결하게 ```solution과  ``` 사이에 작성하라.  영어로 말하라.